## 1. 存储与接口契约

- [x] 1.1 为 JJC 排名统计仓储补充可用赛季枚举与按 `current_season` 读取全部快照元数据的方法，保持既有全局列表方法不变。
- [x] 1.2 为 `action=seasons` 和 `action=season-history&season=` 增加路由参数分发，返回约定的赛季/历史字段并处理空赛季。
- [x] 1.3 编写并执行仓储、路由单元测试，覆盖按最近快照排序及默认赛季、完整单赛季返回、空/不存在赛季，以及既有 `action=list` 分页和响应结构不变。

## 2. 赛季范围前端加载

- [x] 2.1 将页面初始数据加载改为先请求赛季列表，再请求 `default_season` 的完整历史快照；赛季列表为空时展示空状态。
- [x] 2.2 将赛季选择事件改为按请求标识请求所选赛季并重建周次和快照下拉选项；失败时保留最后一次成功状态并显示错误。
- [x] 2.3 补充前端静态行为测试，断言页面不再使用全局最新 100 条接口，使用新增赛季接口，并覆盖空列表、失败与过期响应不覆盖当前选择的行为。

## 3. 验证与交付

- [x] 3.1 运行 JJC 排名仓储、路由和前端测试，以及相关 Python 3.9 兼容性编译检查。
- [x] 3.2 使用本地 Mongo 或部署环境手工验证：默认赛季等于最新快照所属赛季并可见第 1 周，切换上赛季后只显示上赛季快照。
- [x] 3.3 对照规格检查旧列表兼容性、空状态、失败状态与前端快速切换行为，记录验收结果。
