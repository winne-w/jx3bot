# JJC 7 天历史最高分排名 Review 记录

## Review Scope

待实现后核对分层、数据口径、定时任务幂等性、回填脚本安全性、索引设计、文档同步和测试覆盖。

## Findings

未发现阻塞问题。

核对项：

- 分数口径与前端一致：`mmr` 写入 `tuilan_score`，`total_score` 写入 `game_score`，`score` 仅作为兜底。
- 统计窗口使用 `jjc_match_participants.match_time`，该字段来自接口返回的对局发生时间，不使用入库时间。
- 最高分排名聚合从窗口内所有本地 3v3 对局参与者出发，不读取排名快照成员作为统计对象。
- 每个排名项保留最高分对应 `match_id` 和 `match_time`。
- 09:00 job 独立于现有日常推送和 03:00/21:00 心法排名任务。
- 新增 Mongo 集合和投影字段已同步数据库设计文档，历史回填路径已同步 runbook。

## Result

通过。剩余风险是线上历史投影需要执行 apply 回填后，旧对局才会拥有新增拆分分数字段；未回填前最高分统计只会覆盖新写入或已回填的参与者投影。
